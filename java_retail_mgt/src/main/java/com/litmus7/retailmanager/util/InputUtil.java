package com.litmus7.retailmanager.util;

import java.util.Scanner;

public class InputUtil {
    private static Scanner scanner;

    public static String getString(String prompt) {
        // Displays prompt and reads a line of text from the user
        return null;
    }

    public static double getDouble(String prompt) {
        // Displays prompt, reads a double, and handles input mismatch errors
        return 0.0;
    }

    public static int getInt(String prompt) {
        // Displays prompt, reads an int, and handles input mismatch errors
        return 0;
    }

    public static void closeScanner() {
        // Closes the scanner to free resources
    }
}